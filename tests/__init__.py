"""Unit tests for the pcspot package.

Tests use ``unittest`` (stdlib) so they run against the project's
existing CPU-only venv without installing pytest. From the repo root::

    .\\.venv\\Scripts\\python.exe -m unittest discover -s tests -t .
"""
